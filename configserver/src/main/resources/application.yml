server:
  port: 8762
spring:
  application:
    name: configserver
    #从本地读取
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared